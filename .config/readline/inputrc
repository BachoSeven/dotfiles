set bell-style none
# Fixes some buttons when using St
set enable-keypad			on
set colored-stats			on
set visible-stats			on
set mark-symlinked-directories		on
set colored-completion-prefix		on
set blink-matching-paren		on
set comment-begin			#
set completion-prefix-display-length	111
set completion-query-items		200
set input-meta				on
set convert-meta			off
set output-meta				on
set disable-completion			off
set echo-control-characters		on
set match-hidden-files			off
set page-completions			on
set completion-ignore-case		on
set show-all-if-ambiguous		on
set print-completions-horizontally	on

# Tab for printing completion; Shift+Tab for cycling through completions
Tab: complete
"\e[Z": menu-complete

set editing-mode vi
$if mode=vi
	$if Octave
		set show-mode-in-prompt off
		# NOTE: Temporary fix, which won't be necessary starting from Octave version 6.1.0.
		set enable-bracketed-paste off
	$else
		set show-mode-in-prompt on
		set vi-ins-mode-string \1\e[6 q\2
		set vi-cmd-mode-string \1\e[2 q\2

		set keymap vi-command
		# these are for vi-command mode
		Control-l: clear-screen
		Control-a: beginning-of-line

		set keymap vi-insert
		# Just like in my vim config, `kj` enters normal mode.
		"kj": vi-movement-mode
		# switch to block cursor before executing a command
		RETURN: "\e\n"
		# these are for vi-insert mode
		Control-p: previous-history
		Control-n: next-history
		Control-l: clear-screen
		Control-a: beginning-of-line
	$endif

$endif
